// Module 3 – Activity 1 – Calculator Functions
//
// Write FOUR functions, each of which RETURNS a value (it does not print):
//
//   sum(a, b)        -> a + b
//   difference(a, b) -> a - b   (first number minus second)
//   product(a, b)    -> a * b
//   quotient(a, b)   -> a / b   (first number divided by second)
//
// Then, in main(), build a menu program that:
//   1. asks the user for two numbers,
//   2. asks which operation to perform,
//   3. prints the result by CALLING the matching function,
//   4. keeps going until the user chooses to exit.
//
// Use `num` as the type so the functions work for whole numbers and decimals.
// Write it as efficiently as you can: one loop, no copy-pasted blocks.
//
// Concepts to research (see the Module 3 "Functions & Randomness" cheat sheet:
// content/cheat-sheets/dart-m3-functions-and-random.md): functions that return
// a value, reading input, choosing a branch, and a loop that repeats until the
// user exits.
//
// The skeleton compiles. Replace each `0` with the real result; do not rename
// the functions or change their parameters.

num sum(num a, num b) => 0; // TODO

num difference(num a, num b) => 0; // TODO

num product(num a, num b) => 0; // TODO

num quotient(num a, num b) => 0; // TODO

void main() {
  // TODO: ask for two numbers and an operation, print the result by calling the
  // function above, and repeat until the user chooses to exit.
}
